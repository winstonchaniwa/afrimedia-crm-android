# No obfuscation by default (isMinifyEnabled = false). Rules kept here so
# turning minification on later is a one-line change plus these basics.
-keepattributes *Annotation*
-keepclassmembers class com.afrimedia.crm.data.model.** { *; }
